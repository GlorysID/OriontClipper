# CPU-only Torch wheel is required on small VPS hosts; otherwise pip may pull
# very large CUDA dependencies from PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
numpy==1.26.4

# 20231117 can fail to build with newer setuptools build isolation because its
# setup imports pkg_resources. 20250625 fixes that packaging issue while keeping
# the same whisper.load_model/transcribe API used by ContentClipper.
faster-whisper>=1.0
python-dotenv==1.0.1
requests==2.32.3
watchdog==4.0.2

# Telegram Bot + YouTube download (opsional — hanya dibutuhkan jika pakai bot)
python-telegram-bot>=21
yt-dlp>=2026.7.4
